import SignUpForm from "@/components/SignUpForm"

/**
 * Componente SignUpPage
 * 
 * Este componente representa la página de registro de nuevos usuarios en la aplicación. 
 * Se encarga de renderizar el formulario de registro de usuario utilizando el componente `SignUpForm`.
 * 
 * @componente
 * @retorna {JSX.Element} El componente SignUpPage.
 */
export default function SignUpPage() {

  return (
    <>
      <SignUpForm />
    </>
  )
}
